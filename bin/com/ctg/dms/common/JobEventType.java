package com.ctg.dms.common;


public enum JobEventType{
	JOB_EVENT_NEW("JOB_EVENT_NEW"),
	JOB_EVENT_CLOSED("JOB_EVENT_CLOSED"),
    JOB_EVENT_RERAN("JOB_EVENT_RERAN"),
	JOB_EVENT_KILLED("JOB_EVENT_KILLED"),
	JOB_EVENT_INITIATE("JOB_EVENT_INITIATE"),
	JOB_EVENT_ERROR("JOB_EVENT_ERROR"),
	JOB_EVENT_FAILURE_NOTFIED("JOB_EVENT_FAILURE_NOTIFIED"),
	JOB_EVENT_AUTO_RETRY("JOB_EVENT_AUTO_RETRY"),
	JOB_EVENT_RETRY("JOB_EVENT_RETRY"),
	JOB_EVENT_RUNNING("JOB_EVENT_RUNNING"),
	JOB_EVENT_FAILED("JOB_EVENT_FAILED"),
	JOB_EVENT_DONE("JOB_EVENT_DONE"),
	JOB_EVENT_NO_DATA_FOUND("JOB_EVENT_NO_DATA_FOUND"),
	JOB_EVENT_SRQ_FULFILMENT_TEXT_UPDATED("JOB_EVENT_SRQ_FULFILMENT_TEXT_UPDATED"),
	JOB_EVENT_RESPONSE_FILE_UPDATED("JOB_EVENT_RESPONSE_FILE_UPDATED"),
	JOB_EVENT_CSV_FILE_UPDATED("JOB_EVENT_CSV_FILE_UPDATED"),
	JOB_EVENT_JSON_FILE_UPDATED("JOB_EVENT_JSON_FILE_UPDATED"),
	JOB_EVENT_PDF_FILE_UPDATED("JOB_EVENT_PDF_FILE_UPDATED"),
	JOB_EVENT_XML_FILE_UPDATED("JOB_EVENT_XML_FILE_UPDATED"),
	JOB_EVENT_RDHI_FILE_UPDATED("JOB_EVENT_RDHI_FILE_UPDATED"),
	JOB_EVENT_READY_TO_TRANSFORM("JOB_EVENT_READY_TO_TRANSFORM"),
	JOB_EVENT_QUEUED_TO_TRANSFORM("JOB_EVENT_QUEUED_TO_TRANSFORM"),
	JOB_EVENT_TRANSFORM("JOB_EVENT_TRANSFORM"),
	JOB_EVENT_HEARTBEAT("JOB_EVENT_HEARTBEAT"),
	JOB_EVENT_ERROR_RECEIVED_FROM_MRE("JOB_EVENT_ERROR_RECEIVED_FROM_MRE"),
	JOB_EVENT_ERROR_RECEIVED_FROM_PEOPLESOFT_STATUS1_ERRORCODENULL("JOB_EVENT_ERROR_RECEIVED_FROM_PEOPLESOFT_STATUS1_ERRORCODENULL"),
	JOB_EVENT_ERROR_RECEIVED_FROM_PEOPLESOFT_INVALID_STATUS_ERRORCODENULL("JOB_EVENT_ERROR_RECEIVED_FROM_PEOPLESOFT_INVALID_STATUS_ERRORCODENULL"),
	JOB_EVENT_FCR_ERROR_INVALID_DETAIL_PARAMETER("JOB_EVENT_FCR_ERROR_INVALID_DETAIL_PARAMETER");
	
	private String refDataType;
	private JobEventType(String refDataType){
		this.refDataType = refDataType;
	}
	public long getId() {
		return new Long(0).longValue();
 }
	public String getRefDataType() {
		return refDataType;
	}
	public String getValue() {
			return " ";
			}
	public static JobEventType fromRefDataType(String refDataType){
		for (JobEventType type : values()) {
			if(type.refDataType.equals(refDataType)){
				return type;
			}
		}
		return null;
	}
	public static JobEventType fromId(long id){
		for (JobEventType type : values()) {
			if(type.getId() == id){
				return type;
			}
		}
		return null;
	}
}